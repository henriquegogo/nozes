{
  "name": "builder",
  "scripts": {
    "start": "rollup ../products/index.js -o bundle.js -f iife && babel bundle.js -o bundle.js --presets env && cp ../products/index.css ."
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "rollup": "^1.16.6"
  }
}
